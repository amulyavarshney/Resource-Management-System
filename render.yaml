# Render Blueprint — backend API + managed Postgres
# https://render.com/docs/blueprint-spec
#
# Deploy: connect this repo in Render Dashboard → New → Blueprint
# After deploy, set the frontend secret NEXT_PUBLIC_BACKEND_API to
# https://<this-service>.onrender.com/api/v1

databases:
  - name: rms-db
    plan: free

services:
  - type: web
    name: rms-api
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile.render
    dockerContext: ./backend
    healthCheckPath: /health/live
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rms-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_ALGORITHM
        value: HS512
      - key: JWT_EXPIRE_HOURS
        value: "2"
      - key: JWT_REMEMBER_EXPIRE_HOURS
        value: "336"
      - key: APP_ENV
        value: production
      - key: LOG_LEVEL
        value: info
      - key: RATE_LIMIT_ENABLED
        value: "true"
      - key: ALLOW_SELF_REGISTRATION
        value: "false"
      - key: ALLOWED_ORIGINS
        value: '["https://amulyavarshney.github.io","http://localhost:3000"]'
      - key: UVICORN_WORKERS
        value: "2"
      # Optional SMTP — set in the Render dashboard if timesheet mail is needed
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USERNAME
        sync: false
      - key: SMTP_PASSWORD
        sync: false
      - key: SMTP_FROM
        sync: false
      - key: SMTP_FROM_NAME
        value: Resource Management System
      - key: SMTP_REPLY_TO
        sync: false
      - key: SMTP_STARTTLS
        value: "true"
      - key: SMTP_SSL
        value: "false"
