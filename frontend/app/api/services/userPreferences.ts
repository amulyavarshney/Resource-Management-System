export const addToFavourites = async (userId: number, projectId: number) => {
    // Logic to mark the project as favourite for the user
    // This could involve updating a database collection where each document represents a user's favourite projects
};

export const removeFromFavourites = async (userId: number, projectId: number) => {
    // Logic to remove the project from the user's favourite projects
};